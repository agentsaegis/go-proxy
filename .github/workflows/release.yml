name: Release

on:
  push:
    tags:
      - "v*"

permissions:
  contents: write
  actions: read

jobs:
  verify-ci:
    runs-on: ubuntu-latest
    steps:
      - name: Verify CI passed on this SHA
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          COMMIT_SHA: ${{ github.sha }}
          REPO: ${{ github.repository }}
        run: |
          echo "Checking CI status for $COMMIT_SHA on $REPO"
          CONCLUSION=$(gh api "repos/$REPO/commits/$COMMIT_SHA/check-runs" \
            --jq '.check_runs[] | select(.name == "ci") | .conclusion' 2>/dev/null || true)
          if [ "$CONCLUSION" != "success" ]; then
            echo "::error::CI has not passed on $COMMIT_SHA (got: ${CONCLUSION:-not found}). Push to main and wait for CI before tagging."
            exit 1
          fi
          echo "CI passed on $COMMIT_SHA"

  goreleaser:
    needs: [verify-ci]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - uses: actions/setup-go@v5
        with:
          go-version-file: go.mod

      - uses: goreleaser/goreleaser-action@v6
        with:
          version: "~> v2"
          args: release --clean
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          HOMEBREW_TAP_TOKEN: ${{ secrets.HOMEBREW_TAP_TOKEN }}
